import Banner from "../components/Banner/Banner";
//import Collapse from "../components/Collapse/Collapse";

function Home() {
  return (
    <>
      {/* <div className="About">
        <main>
          <Banner origin="about" />
          <section className="dropdown">
            {collapse.map((text, idx) => {
              return (
                <Collapse
                  title={text.title}
                  key={idx}
                  description={text.description}
                  class="dropdown_container"
                />
              );
            })}
          </section>
        </main>
      </div> */}
      <div>TODO voir collapse</div>
    </>
  );
}

export default Home;
